---
- hosts: all
  become: yes
  tasks:
  - name: I want to install web server on Ubuntu
    apt: 
      name: apache2
      state: present
  - name: Start the webserver
    service:
      name: apache2
      state: started
